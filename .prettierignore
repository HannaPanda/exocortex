node_modules
dist
.next
.turbo
coverage
pnpm-lock.yaml
packages/database/generated
packages/database/prisma/migrations
playwright-report
test-results

# Written by scripts/generate-*-data.mjs. Reformatting a half-megabyte string
# literal costs seconds and no one reads it.
**/*.generated.ts
packages/contracts/src/lucide-icon-names.ts

# Rewritten by `next build` on every run, in Next's own style ("should not be
# edited" is the second line of the file). Formatting it means the build and the
# format check undo each other's work forever: the build restores the double
# quotes, and the next check reports the file as unformatted.
apps/web/next-env.d.ts

# Generated by scripts/check-capability-parity.mjs, and compared against a fresh
# render by the gate of the same name. Formatting it would put the two in a loop:
# the gate rewrites the table, the format check pads it back out again.
docs/capability-matrix.md

# A verbatim copy of Vercel's Web Interface Guidelines at a pinned commit. Its
# blob hash is what `update.sh` compares against upstream, so reformatting it
# would make every future update look like a change nobody made.
.claude/skills/web-design-guidelines/guidelines.md

# Generated by scripts/generate-oxlint-config.mjs, and compared against a fresh
# render by the package-boundary gate. Same loop as the matrix above: Prettier
# would collapse the arrays, the gate would expand them again.
.oxlintrc.json
