{
  "mcpServers": {
    "exocortex": {
      "type": "http",
      "url": "${user_config.api_url}/api/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.api_token}"
      }
    }
  }
}
