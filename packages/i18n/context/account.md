The account's connection and notification settings under /einstellungen: the list of connected applications (OAuth clients such as ChatGPT) with disconnecting, the personal API tokens with their rights, expiry and page limits, the dialog that shows a new token once, the setup cards with ready-to-copy commands for Claude Code, ChatGPT, Hermes and other MCP clients, and the notification settings per device (push) and per account (mail). Headings, table columns, badges and options are short labels; explanations and confirmations are full sentences addressed to the person with "du" that say plainly what a right allows and what cannot be undone. Client names, `{placeholder}` values, file paths, command names and flags such as `--scope user` stay exactly as they are.
